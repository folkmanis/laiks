import { SelectorColorDirective } from './selector-color.directive';

describe('SelectorColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectorColorDirective();
    expect(directive).toBeTruthy();
  });
});
