import { HourDetailsDirective } from './hour-details.directive';

describe('HourDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new HourDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
