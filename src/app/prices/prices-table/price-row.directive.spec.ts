import { PriceRowDirective } from './price-row.directive';

describe('PriceRowDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceRowDirective();
    expect(directive).toBeTruthy();
  });
});
