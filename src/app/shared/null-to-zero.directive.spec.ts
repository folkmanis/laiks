import { NullToZeroDirective } from './null-to-zero.directive';

describe('NullToZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new NullToZeroDirective();
    expect(directive).toBeTruthy();
  });
});
