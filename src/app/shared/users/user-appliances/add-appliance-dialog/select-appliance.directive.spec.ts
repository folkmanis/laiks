import { SelectApplianceDirective } from './select-appliance.directive';

describe('SelectApplianceDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectApplianceDirective();
    expect(directive).toBeTruthy();
  });
});
